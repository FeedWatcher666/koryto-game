name: Koryto v0.17.4 TEST.1

on:
  push:
    branches:
      - agent/v0174-style-stabilization
  pull_request:
    branches:
      - agent/v0173-playability-reset
      - agent/v0172-turn-clarity
      - agent/v0171-decision-hud
      - agent/v0170-visual-foundation
      - agent/v0160-clean-ui
      - fix/pr19-v0149-wiring
      - test/v0.14.8-playable
      - main
  workflow_dispatch:

jobs:
  test-and-package:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Run tests
        shell: bash
        run: |
          set -o pipefail
          npm test 2>&1 | tee test-output.txt

      - name: Upload test diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: koryto-v0.17.4-test.1-diagnostics
          path: test-output.txt
          if-no-files-found: warn

      - name: Build offline package
        shell: bash
        run: |
          set -euo pipefail
          build_version="$(cat VERSION)"
          test "$build_version" = "0.17.4-test.1"
          target="dist/koryto-v${build_version}"
          mkdir -p "$target"
          cp index.html VERSION README.md "$target/"
          cp -R src styles assets "$target/"
          mkdir -p "$target/docs/v0.14" "$target/docs/v0.16" "$target/docs/v0.17"
          cp docs/v0.14/v0146-playtest-report.md "$target/docs/v0.14/"
          cp docs/v0.14/v0147-consequence-report.md "$target/docs/v0.14/"
          cp docs/v0.16/v0160-clean-ui.md "$target/docs/v0.16/"
          cp docs/v0.16/v0161-layout-polish.md "$target/docs/v0.16/"
          cp docs/v0.16/v0162-playtest.md "$target/docs/v0.16/"
          cp docs/v0.16/v0163-staff-polish.md "$target/docs/v0.16/"
          cp docs/v0.16/v0165-audit.md "$target/docs/v0.16/"
          cp docs/v0.16/v0166-day-debate.md "$target/docs/v0.16/"
          cp docs/v0.16/v0166-browser-smoke.md "$target/docs/v0.16/"
          cp docs/v0.16/v0167-election-coalition.md "$target/docs/v0.16/"
          cp docs/v0.16/v0168-onboarding.md "$target/docs/v0.16/"
          cp docs/v0.16/v0169-release-polish.md "$target/docs/v0.16/"
          cp docs/v0.16/v0169-test3.md "$target/docs/v0.16/"
          cp docs/v0.17/v0170-visual-foundation.md "$target/docs/v0.17/"
          cp docs/v0.17/v0171-decision-hud.md "$target/docs/v0.17/"
          cp docs/v0.17/v0172-turn-clarity.md "$target/docs/v0.17/"
          cp docs/v0.17/v0173-playability-reset.md "$target/docs/v0.17/"
          cp docs/v0.17/v0174-style-stabilization.md "$target/docs/v0.17/"
          rm -f "$target/src/v0142-rc2.js" "$target/src/v0142-rc3.js"

          test -f "$target/src/build-info.js"
          test -f "$target/src/v0168-onboarding-ui.js"
          test -f "$target/src/v0169-release-polish.js"
          test -f "$target/src/v0169-map-stability.js"
          test -f "$target/src/v0169-viewport-lock.js"
          test -f "$target/src/v0170-visual-foundation.js"
          test -f "$target/src/v0171-decision-hud.js"
          test -f "$target/src/v0172-turn-clarity.js"
          test -f "$target/src/v0173-playability-reset.js"
          test -f "$target/src/v0174-style-stabilization.js"
          test -f "$target/styles/v0168-onboarding-ui.css"
          test -f "$target/styles/v0169-release-polish.css"
          test -f "$target/styles/v0169-map-stability.css"
          test -f "$target/styles/v0169-viewport-lock.css"
          test -f "$target/styles/v0169-creation-flow-fix.css"
          test -f "$target/styles/v0170-visual-foundation.css"
          test -f "$target/styles/v0171-decision-hud.css"
          test -f "$target/styles/v0172-turn-clarity.css"
          test -f "$target/styles/v0173-playability-reset.css"
          test -f "$target/styles/v0174-style-stabilization.css"
          test -f "$target/assets/v0164/map-pixel-cartoon.webp"
          test -f "$target/assets/v0165/manifest.json"

          grep -q 'viewport-fit=cover' "$target/index.html"
          grep -q 'Koryto 0.17.4 TEST.1' "$target/index.html"
          grep -q 'src/build-info.js' "$target/index.html"
          grep -q 'src/v0168-onboarding-ui.js' "$target/index.html"
          grep -q 'src/v0169-release-polish.js' "$target/index.html"
          grep -q 'src/v0169-map-stability.js' "$target/index.html"
          grep -q 'src/v0169-viewport-lock.js' "$target/index.html"
          grep -q 'src/v0170-visual-foundation.js' "$target/index.html"
          grep -q 'src/v0171-decision-hud.js' "$target/index.html"
          grep -q 'src/v0172-turn-clarity.js' "$target/index.html"
          grep -q 'src/v0173-playability-reset.js' "$target/index.html"
          grep -q 'src/v0174-style-stabilization.js' "$target/index.html"
          grep -q 'styles/v0169-creation-flow-fix.css' "$target/index.html"
          grep -q 'styles/v0170-visual-foundation.css' "$target/index.html"
          grep -q 'styles/v0171-decision-hud.css' "$target/index.html"
          grep -q 'styles/v0172-turn-clarity.css' "$target/index.html"
          grep -q 'styles/v0173-playability-reset.css' "$target/index.html"
          grep -q 'styles/v0174-style-stabilization.css' "$target/index.html"
          if grep -E 'v0162-playtest\.(css|js)|loadPlaytestLayer|playtestRequested' "$target/src/v0161-interaction-guard.js"; then
            echo "Release still contains the retired playtest loader" >&2
            exit 1
          fi
          if grep -R -E 'https?://(fonts|cdn|unpkg|jsdelivr)' "$target/index.html" "$target/styles" "$target/src"; then
            echo "Release contains a forbidden network dependency" >&2
            exit 1
          fi

      - name: Install Chromium gate dependency
        shell: bash
        run: |
          npm install --no-save playwright@1.54.2
          npx playwright install --with-deps chromium

      - name: Browser gate on packaged artifact
        shell: bash
        run: |
          # v0171-browser-gate.mjs is adapted to the current release by this wrapper.
          node tests/v0174-browser-gate.mjs dist/koryto-v0.17.4-test.1

      - name: Upload browser gate screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: koryto-v0.17.4-test.1-browser-gate
          path: browser-artifacts
          if-no-files-found: warn

      - name: Upload playable build
        uses: actions/upload-artifact@v4
        with:
          name: koryto-v0.17.4-test.1
          path: dist/koryto-v0.17.4-test.1
          if-no-files-found: error
