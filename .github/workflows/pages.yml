name: Deploy green Koryto tester

on:
  workflow_run:
    workflows:
      - Koryto clean rewrite
    types:
      - completed
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: koryto-pages
  cancel-in-progress: true

jobs:
  build:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success' &&
       github.event.workflow_run.event == 'push' &&
       github.event.workflow_run.head_branch == 'agent/v0200-clean-rewrite')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact green commit
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
          persist-credentials: false
      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          package-manager-cache: false
      - name: Build offline tester
        run: node scripts/build-offline.mjs
      - name: Prepare Pages root
        shell: bash
        run: |
          set -euo pipefail
          rm -rf _site
          cp -R dist/koryto-v0.20.0-clean-test.6 _site
          touch _site/.nojekyll
          cat > _site/tester-build.json <<JSON
          {
            "version": "0.20.0-clean-test.6",
            "commit": "${GITHUB_SHA}",
            "workflowRun": "${GITHUB_RUN_ID}"
          }
          JSON
      - name: Configure Pages
        uses: actions/configure-pages@v6
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy tester
        id: deployment
        uses: actions/deploy-pages@v4
