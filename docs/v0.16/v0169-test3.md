# Koryto v0.16.9 TEST.3 — release integrity closure

## Scope

Krátká opravná iterace bez nových questů, mechanik, grafických předělávek nebo změny balance. Save zůstává `0.14.3-test.2`, schema `1`.

## Opravy

- každý aktivní povrch v0.16.5 skutečně skryje starou v0.14.8 navigaci a nastaví jí `inert` a `aria-hidden`,
- patička získává display verzi pouze z `KorytoBuildInfo`; opakované volání je idempotentní,
- nepodporovaný `?playtest=1` loader a jeho produkční odkazy byly odstraněny,
- packaged Chromium gate kontroluje navigaci v události, výsledku, lokaci i mapě,
- packaged Chromium gate provádí skutečný roundtrip `uložit → reload → načíst → porovnat stav`.

## Release pravidlo

PR zůstává draft. Po úspěšném CI musí následovat nové review přesného finálního SHA a dvě kompletní lidské kampaně: desktop a mobil.
